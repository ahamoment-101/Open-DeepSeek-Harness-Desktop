# T2-DESIGN.md — T2 产品设计基线（插件编排 + 自进化）

> T2 的正式设计基线。配合 `KNOWHOW.md`（架构）与 `IMPLEMENTATION.md`（T1 施工）阅读。
> 本文记录的是「我们要做什么、为什么、怎么落地」，不含具体实现代码。

---

## 1. 核心主张（一句话）

**T2 的护城河不是「插件市场」，而是「插件编排 + 自定义 agent」。** 市场只是货架；编排（把积木连成流程）与预设（把流程固化成可复用的 agent）才是价值所在。企业级尤其如此——一套「标准 agent」= 一个组织的工程规范被代码化。

---

## 2. 三层能力模型（skill / tool 插件 / event-hook 插件）

这是 T2 一切的分类基础。三者边界清晰：

| 层 | 承载什么 | 确定性 | 形态 | 风险 |
|---|---|---|---|---|
| **Skill** | 经验 / 增量披露的上下文（让模型「读」） | 低（模型可用可不用） | 提示词 + 步骤 + 可选脚本 | 最低 |
| **Tool 插件** | 缺失的硬能力（让模型「调」） | 中（模型决定何时用） | 代码 `execute` | 中 |
| **Event/Hook 插件** | 硬卡点 / 强制护栏（系统每次必跑） | **高**（系统强制，模型绕不过） | 事件监听 | 中高 |

**关键区分**：
- **Skill 教「怎么做」，Plugin 给「能做什么」。能教会的，绝不新造器官。**
- Plugin 内部还分三种（工具 / 服务 / 事件监听）；「硬卡点」对应的是**事件监听**（确定性），不是工具（模型选）。
- 企业需求的三类落点：规范 → **skill**；缺失能力 → **tool 插件**；强制护栏 → **event/hook 插件**。

---

## 3. 三块组成

### 3.1 市场（插件 + skill 仓库）
- 用户可创建、发布、分享插件与 skill。
- **skill 市场可松**（文本、低风险、用户可写）；**插件市场必须严**（可执行代码，需审查/签名/沙箱）。
- 市场本身不构成护城河——它是「积木仓库」。

### 3.2 DAG 编排引擎（把积木连成流程）
- 本质：**给 harness 已经存在的「图」配一个可视化编辑 + 编译层**，而非从零写引擎。
- harness 底层已经是图：`inject`（依赖）、事件监听（`agent/*` / `tools/*` / `fs/*`）、工具注册、组合启动，且自带自动生成的 service/consumer DAG（`docs/capability-seams.md`）与事件生产者/消费者映射（`docs/event-producer-consumer.md`）。
- 用户视角的「触发器」= 四种原生机制：**注入依赖 / 监听事件 / 模型调用 / 组合启动**。
- 引擎把用户的可视化编排**编译回** `inject` + 事件监听 + 工具注册。

**两个约束（必须写进产品承诺）**：
1. **触发词表是固定的**：可编排的点受限于 harness 已类型化的事件集；不能凭空声明全新触发点（新触发点 = 写一个「发出新事件的插件」）。这是安全边界。
2. **别与 harness 的 `workflow` 混**：`workflow` 包（`tool-workflow`/`tool-ralph`）是「agent 驱动的结构化多步工作流」，与本 DAG 引擎（静态工程层插件编排）是两回事，命名与文档必须区分。

### 3.3 自定义 agent 创建
- 创建时默认一个基础插件流程；用户在其中**任意穿插插件**；也可**引入一个已编排好的 DAG 工作流**。
- 底层对应 harness 的 **agent preset**：`preset.yml`（name/description/order）+ `agent.cordis.yml`（实际组合清单），用 `isolate` realm 做 agent 级隔离。
- **隔离承诺**：不同 agent 的编排互不影响（scope/preset 逻辑隔离；子 agent 还支持进程级隔离）。

**三者关系**：`市场（积木）→ DAG 编排（连线）→ 预设（实例）`，是「积木 → 连线 → 实例」的完整链路，每一环都踩在 harness 原生机制上。

---

## 4. 自进化飞轮（历史 → skill → preset → 共享）

```
会话历史（session log，追加式、可审计）
   ↓ 挖掘：重复检测 + 聚类 + 用户确认
用户固定模式 / 经验
   ↓ 封装（几乎总是 skill，非插件）
Skill（可复用经验）
   ↓ 编排
Preset（企业标准 agent）
   ↓ 分享
团队共享 → 被更多对话使用 → 产生更多可挖素材（正反馈）
```

- **「经验/模式」几乎永远封装成 skill，不是插件**（经验是知识，不是新能力；只有当挖出的是「目前不存在、需新代码的新工具」时才升级成 plugin）。
- **落地顺序**：手动 skill/preset → 提议模式（agent 提议、用户确认）→ 半自动（版本化 + 可回滚 + 权限门槛）。
- **自进化的三个真坑**（不是技术，是产品）：
  1. 信号 vs 噪音：历史 90% 是噪音，靠重复检测 + 用户确认，不静默自动安装；
  2. 信任：必须是「agent 提议、用户确认」，人在环内（企业级非谈判项）；
  3. 反馈闭环：怎么知道一个 pattern 是好的（harness 有 `message-feedback`，但信号稀疏，要做扎实）。

---

## 5. 三大难点（提前亮牌）

1. **可视化易用性**：让非工程师看懂「inject / 事件 / 工具」图并安全连线——最费力气、也最值钱。
2. **第三方插件安全层**：插件是可执行代码，需审查/签名/沙箱 + 审批管线（skill 市场松、插件市场严）。
3. **编排校验**：连错（循环依赖、依赖不存在）要启动时**响亮失败**（fail loud），不能悄悄跑错——harness 的 fail-loud 哲学已打底。

---

## 6. 与 harness 原生机制映射（速查）

| T2 概念 | harness 原生 |
|---|---|
| 插件 | Cordis 插件（`name` + `inject` + `apply` + 注册） |
| skill | `ctx.skills` + `skill-filesystem` + `tool-skill`（按需加载目录/正文） |
| tool 插件 | `ctx.tools.register(defineTool(...))` |
| event/hook 插件 | 事件监听（`agent/*`、`tools/*`、`fs/*`）；`hooks-claude-code`/`hooks-codex` 兼容 |
| 服务依赖（下游） | `inject: [...]` |
| 组合启动 | `cordis.yml` 行 / bundle patch |
| 预设 | agent preset（`preset.yml` + `agent.cordis.yml` + `isolate` realm） |
| 编排引擎要编译的目标 | `inject` + 事件监听 + 工具注册 |
| 会话历史（自进化原料） | session log（`session-query` 全文搜索、`session-reference` 跨会话快照） |

---

## 7. 落地顺序（建议）

1. **手动 skill + 手动 preset**（零风险，证明「终点」有价值，harness 已支持，做 UX）。
2. **提议模式自进化**（agent 挖历史、提议存 skill、用户一键确认）。
3. **半自动自进化**（有足够确认数据 + 反馈后，加版本化/回滚/权限门槛）。

---

*本文档于 2026-08-16 基于产品讨论定稿。T2 具体实现前，如有新结论回写本文件。*
