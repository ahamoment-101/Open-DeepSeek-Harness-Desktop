# Flat node_modules (npm-style) so electron-builder can collect every
# dependency — including pnpm-managed peerDependencies, which its symlinked
# layout otherwise drops from the packaged app.
node-linker=hoisted
